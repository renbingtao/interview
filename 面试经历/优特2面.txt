Object有哪些常用方法
equals,hashcode,toString,wait,notify,notifyAll,finalize

java异常处理是怎样的

finally和return的优先级

jvm的内存模型是怎样的

本地方法栈是线程私有的吗

直接内存有什么使用场景
NIO

直接内存发生泄露，要如何观察

堆由哪几块组成

说下young gc的过程

HashMap的数据结构

hashmap的扩容，在java7和8有什么差异
计算下标的方式，7是hash对扩容后的长度-1取模；8是hash对扩容前长度取模，判断是否为0

hashmap是线程安全的吗？有哪些线程安全的集合

concurrentHashMap在1.8是如何实现的

如何基于hashMap实现安全的集合，说下伪代码

什么是原子操作
一系列操作不可被分割，要么都执行，要么都不执行，不会出现部分执行的中间状态，执行过程中不能被其他线程中断

i++属于吗

如何实现安全的i++
原子类

除了这个呢，传统的方法
sync锁；volatile+cas

mysql分页怎么做的

索引失效的情况

聚簇索引和非聚簇索引的区别
聚簇索引的索引和数据存储在一起的，叶子节点包含了数据
非聚簇索引索引和数据是分开的，叶子节点包含了主键值

一个表可以建两个聚簇索引吗

什么是索引下推

行锁和表锁有什么区别

写一条什么样的sql，它会用到间隙锁

你们项目中用的是自增id还是UUID？有了解过UUID吗

分布式id是如何实现的

雪花算法的序列号不够用怎么办
最常用的方案：等待至下一毫秒；增加机器数量
其他方案：增加序列号的位数；低峰期预先生成一批id并缓存，高峰期时使用；临时改为UUID或数据库自增；

zookeeper的watch和watcher有什么区别
watch是事件，当监听的事件发生时，会通知这个watch的client，就是watcher

znode的4种形式是什么

zookeeper做配置中心注册中心，是怎么做的
配置文件保存在特定znode节点下；客户端启动时从这个节点拉去全量配置，同时注册watcher，当配置变更时，zk会通知客户端，实现动态更新
选择一个持久化znode节点作为服务的根节点。服务提供者启动时，在服务根节点下创建临时节点，保存服务实例的信息，比如ip端口。服务消费者启动时读取并监听服务根节点下的临时节点，服务上线或下线时更新本地的服务列表

什么是CAP

rabbitmq用来做什么功能
定义业务事件，发生时封装成消息发送到mq，其他模块可以订阅，从而实现解耦

如何提升消费者消费的速度

kafka重平衡的过程

kafka能够减少partition吗
默认情况下不允许直接删除已有分区。如果一定要做，也是建议新建一个分区数更少的topic，并将旧topic的数据迁移到新的topic上
延展：为什么不允许删除？删除分区的数据要如何处理？直接删除会导致丢消息；分配给其他分区，如果保存在末尾会破坏顺序性（时间排序），如果按时间排序会重新复制消息，开销很大。此外还要考虑事务等

kafka如何保证生产者和消费者的消息可靠性

ELK监控的日志是程序日志还是操作日志

mysql的上机日志量级多大？数据量很大的话如何提高查询效率

大表如何分区
