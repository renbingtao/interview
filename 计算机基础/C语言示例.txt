#include <stdio.h>

//定义数组及初始化
int myArr[100];
memset(myArr,0,100);

//循环
for(int i=0;i<100;i++){
    myArr[i]=xxx;
    if(true){
        printf("%c");
    }
}

定义链表结点
typedef struct node{
    int value;
    struct node *pre,*next;
}Node
其中node为【标签名】，Node为【别名】
node中引用自己时，只能使用【标签名】
